---
description: 
alwaysApply: true
---

---
alwaysApply: true
---

# 客户端文本处理规则

客户端文本分为三类。

**数据来源总则：** 多语言字典绝大部分由服务器下发。客户端本地 JSON 仅用于「与服务器建立连接之前」必须显示的那一小部分文案（如连接中、加载中、超时提示等），不得用 JSON 承担连接后/登录前/登录后界面的完整字典。

---

## 1. 开发者日志（Developer Logs）

适用范围：Utils.Debug.Log / LogWarning / LogError / LogSuccess / LogHeartbeat。受众：开发者、测试。语言：纯英文。处理方式：直接硬编码英文字符串。禁止：中文、本地化系统。

---

## 2. 连接前用户文本（Pre-Connection User Texts）

适用范围：在与服务器取得连接之前必须展示的极少文案（如 Dark 的“连接中”、超时/网络错误提示等）。受众：玩家。语言：多语言。处理方式：Localization.Instance.Get(key)。数据来源：仅客户端本地 JSON（Resources/Localization_{language}.json），且仅包含上述连接前必需项。组合动态数据时用格式化参数，禁止字符串拼接中文。

---

## 3. 连接后用户文本（Post-Connection User Texts）

适用范围：与服务器建立连接之后的所有界面（含 Start、Account、设置等登录前界面，以及 Home、Option、Story 等登录后界面）。受众：玩家。语言：多语言。处理方式：从服务器下发的多语言字典按 key 取文案（如 DataManager 的 Texts 或协议下发的字典）；登录后界面按 SDUI 由服务器通过 Protocol 推送。数据来源：服务器，不是本地 JSON。核心规则：连接后界面不依赖本地 JSON 作为主字典；登录后游戏内 UI 不硬编码，由服务器推送；客户端只负责按 key 取字典或渲染协议数据。

---

## 总结

开发者日志：数据在代码内，英文硬编码。连接前 UI：数据来自本地 JSON（极少量），Localization.Instance.Get(key)。连接后 UI：数据来自服务器下发的字典/Protocol，按 key 取字典或渲染协议数据。

硬性禁止：代码中直接写中文字符串（日志、UI、提示等）；代码中直接写英文 UI 文本（仅日志允许）；用本地 JSON 承担连接后/登录前/登录后界面的完整多语言字典；字符串拼接中文内容。
