---
alwaysApply: true
---

# Unity 客户端热更新程序集架构规则

本项目使用 HybridCLR 实现热更新。**目标：版本上线后实现 0 强更**（无需重新提审即可更新游戏功能与内容）。

**核心原则**：程序集划分的**唯一依据**是**是否可在上线后修改**，而非代码组织是否优雅。

---

## 程序集分类

### 1. Assembly-CSharp（启动引导）

**位置**：`Assets/Bootstrap/`（无 `.asmdef`，编译到 Unity 默认的 `Assembly-CSharp.dll`）

- **职责**：应用启动与热更新引导
- **包含**：启动入口（Main.cs）、版本检查、DLL 下载、动态加载 Game 程序集
- **约束**：
  - ❌ 不得包含业务逻辑
  - ❌ 不得包含完整 UI（启动进度条除外）
  - ✅ 只负责"把游戏启动起来"

---

### 2. Framework（基础设施）

**定义**：`Assets/Framework/Framework.asmdef`

- **职责**：为 Game 程序集提供稳定、长期不变的基础能力
- **包含**：资源管理、网络基础设施（Http/Socket）、文件操作、Editor 需要的工具、第三方 SDK 封装
- **约束**：
  - ✅ API 需谨慎设计，考虑长期稳定性
  - ❌ 不得包含业务逻辑
  - ⚠️ 修改 Framework 需重新打包提审

---

### 3. Game（业务逻辑 - 可热更新）

**定义**：`Assets/Game/Scripts/Game.asmdef`

- **职责**：承载所有可能频繁迭代的游戏功能
- **包含**：所有 UI、游戏流程、网络协议业务处理、数据管理、玩法系统
- **约束**：
  - ✅ 所有业务逻辑必须放在 Game 程序集
  - ✅ 确保任何业务调整可通过热更新完成

---

### 4. Editor（开发工具）

**位置**：`Assets/Editor/`（无 `.asmdef`，自动编译为 Editor 程序集）

- **职责**：Unity 编辑器扩展、构建工具、开发辅助工具
- **包含**：自定义编辑器窗口、Inspector 扩展、构建脚本、资源处理工具、协议生成工具
- **约束**：
  - ✅ 可以引用 Framework 和 Assembly-CSharp
  - ❌ **不能引用 Game**（Game.dll 运行时才加载）
  - ⚠️ 只在编辑器中运行，不参与打包

---

## 依赖规则

### 依赖图

```
Editor ──┐
         ├─> Framework <── Game（热更新）
Bootstrap─┘                 ↑
         └──[反射加载]───────┘
```

### 依赖表

| 程序集 | 可以依赖 | 不得依赖 |
|--------|----------|----------|
| **Editor** | Framework、Bootstrap | Game |
| **Bootstrap** | Framework | Game、Editor |
| **Framework** | 第三方库 | Game、Editor、Bootstrap |
| **Game** | Framework | Bootstrap、Editor |

**核心约束**：
- ❌ 禁止循环依赖
- ❌ Bootstrap 不能静态引用 Game（通过 `Assembly.Load()` 动态加载）
- ❌ Editor 不能引用 Game（Game.dll 编辑时不存在）

---

## 决策树：代码应该放在哪里？

### 第 1 步：Editor 需要使用吗？
- **是** → **Framework**（Editor 无法访问 Game）
  - 示例：构建工具（ZipSharp、DirectoryUtils）、协议生成器
- **否** → 继续第 2 步

### 第 2 步：是否只用于启动？
- **是** → **Bootstrap**（Assembly-CSharp）
  - 示例：Main.cs、热更新下载器、启动配置
- **否** → 继续第 3 步

### 第 3 步：上线后会频繁修改吗？
- **会，经常改** → **Game**
  - 示例：UI、玩法、业务逻辑、网络协议处理
- **不会，稳定基础设施** → **Framework**
  - 示例：资源管理器、HTTP 封装、文件工具、SDK 封装

---

## 特殊场景处理

| 场景 | 解决方案 | 原因 |
|------|----------|------|
| `game.dll.bytes` > 5MB | 分析是否有稳定代码可下沉至 Framework | 保持热更新包体积小 |
| Framework 能力不足 | 短期：Game 实现或绕行；长期：规划到下一主版本 | 不污染 AOT 程序集 |
| Editor 和 Game 都需要的工具 | **必须放 Framework** | Editor 只能访问 Framework |

---

## 核心理念

**在热更新架构中，"是否可热更"永远优先于"代码组织是否优雅"。**
