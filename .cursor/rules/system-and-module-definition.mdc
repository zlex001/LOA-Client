---
alwaysApply: true
---

# 术语定义：Manager、System 与 Module

为避免歧义，本项目中的术语定义如下：

- Manager（管理器）：继承 Singleton<T> 的核心单例类或按项目约定命名的管理类，如 Data、Net、UI；或 {功能}Manager（如 SDKManager）。对应程序集中承担数据、网络、界面等核心职责的单例入口。
- System（系统）：功能模块的统称，用于描述一组相关能力或文档主题，如热更新系统、本地化系统、认证系统。可对应 Logic 等层的多个类或跨层协作，不指代单个程序集或命名空间。
- Module（模块）：某个 Manager 或 System 下的具体功能类、子模块或核心 .cs 文件。文档中二级标题（##）与模块一一对应。

使用约束：

- 不得将程序集（Assembly）称为 System
- 不得将命名空间（Namespace）称为 System
- 讨论“管理器 / 系统 / 模块”时，必须以上述定义为准
