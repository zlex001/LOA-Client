---
description: 
alwaysApply: true
---

# 系统设计文档撰写规则（Documents/System）

适用范围：
- Documents/System 目录下的所有系统设计文档（.md）

结构规则：

1) 一级标题（#）定义"系统名称/管理器名称"。

2) 从一级标题（#）之后开始，到第一个二级标题（##）之前的内容，统一视为"概述区"：
- 用于描述该系统或管理器的核心职责、设计理念、与其他系统的协作方式
- 对于管理器（如Data、Net、UI），说明其在管理器协作架构中的定位
- 对于功能系统（如热更新系统、本地化系统），说明其核心功能和使用场景

3) 每一个二级标题（##）对应该系统的一个"模块"或"核心类"：
- 对于管理器：二级标题对应管理器的核心类或子模块（如Data.cs、Config.cs、Local.cs）
- 对于功能系统：二级标题对应系统的各个功能模块
- 二级标题命名应与类文件名或模块职责一致（建议可追溯到具体 .cs 文件）
- 模块的设计说明必须写在对应的二级标题下，不得散落在其他模块章节

语言与格式：

1) 章节标题使用中文，必要时附英文术语：
- 格式：`## 中文名称（EnglishTerm）`
- 示例：`# 数据管理系统（Data）`、`## 配置数据（Config）`

2) 术语定义与引用：
- 术语在首次出现时使用"中文（English）"格式定义
- 后续引用时使用加粗英文格式（**Term**）
- 若术语有独立章节，引用时添加跳转链接：[**Term**](#章节锚点)
- 锚点格式：标题转小写，括号移除，中英文连续（如 `### 配置数据（Config）` → `#配置数据config`）
- 术语的英文命名应与代码中的命名保持一致

3) 名词解释：
- 格式：**中文名**（代码方法/属性名/类名）是...描述...
- 用途：将流程图节点或核心功能与代码中的实际实现映射
- 位置：流程图下方，或概述区
- 括号内的英文必须与代码中的方法名、属性名或类名一致
- 示例：**初始化方法**（Init）是在 `Gate.Entrance` 中调用的管理器初始化方法...

4) 正文使用中文撰写

5) 客户端核心概念：
- Manager（管理器）：继承 Singleton<T> 的核心单例类，如 Data.Instance、Net.Instance、UI.Instance
- System（系统）：功能模块的统称，如热更新系统、本地化系统
- Module（模块）：系统或管理器内的具体功能类或子模块
