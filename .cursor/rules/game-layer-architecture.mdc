---
alwaysApply: true
---

# LOA 客户端分层架构规则（Layered Architecture Rules v4.0）

LOA 客户端采用与服务端对齐的严格分层架构。代码组织为 8 个独立程序集（Assembly），通过 .asmdef 强制执行显式依赖约束。依赖关系从高层单向流向低层，反向通信仅使用事件机制。

**完整架构说明：** `Documents/Architecture/客户端架构文档.md`

---

## 一、层级总览（Layer Overview）

```
Utils（工具层）    Basic（基础设施层）
         \        /
       Data（数据层）
             ↓
       Net（网络层）
             ↓
      Logic（逻辑层）
             ↓
   Presentation（表现层）
             ↓
      Start（启动层）

SDK（独立，仅依赖 Framework）
```

> 所有热更新程序集目录前缀为 `Assets/HotScript/`。每个程序集对应独立命名空间，命名规则为 `Game.{层名}`（如 `Game.Data`、`Game.Net`）。Framework 程序集使用 `Framework` 命名空间。ThirdParty/ 位于 `Assets/ThirdParty/`，保留原始命名空间。

| 层 | 程序集 → 目录 | 命名空间 | 依赖 | 目录结构 |
|---|---|---|---|---|
| Utils | `Utils` → `Utils/` | `Game.Utils` | Framework, SuperScrollView | 按功能建子目录（Extension/, Time/, Wheel/） |
| Basic | `Basic` → `Basic/` | `Game.Basic` | Framework | 平铺 |
| Data | `Data` → `Data/` | `Game.Data` | Utils, Basic, Framework, LitJson | 数据模型放 Models/，其余根目录 |
| Net | `Net` → `Net/` | `Game.Net`（协议：`Game.Net.Protocol`） | Data, Basic, Framework, Utils | 协议放 Protocol/，其余根目录 |
| Logic | `Logic` → `Logic/` | `Game.Logic` | Net, Data, Utils, Basic, Framework | 平铺 |
| Presentation | `Presentation` → `Presentation/` | `Game.Presentation` | Logic, Data, Utils, Basic, Framework, Net, LitJson, DOTweenModules, SuperScrollView, Unity.Addressables, UnityEngine.Purchasing | 平铺 |
| Start | `Start` → `Start/` | `Game.Start` | Framework, Basic, Data, Net, Logic, Presentation | 平铺 |
| SDK | `SDK` → `SDK/` | `Game.SDK` | Framework | 平台实现按 {Platform}/ 子目录 |

---

## 二、依赖与程序集规则（Dependency and Assembly Rules）

**高层 → 低层：** 直接调用

```csharp
var home = Data.Instance.Home;      // ✓ Presentation → Data
Data.Instance.Home = homeData;      // ✓ Net → Data
```

**低层 → 高层：** 禁止直接调用，使用以下机制：

```csharp
// Event (Game.Basic.Event)
Event.Instance.Add("EventName", callback);   // register (upper)
Event.Instance.Fire("EventName", args);       // fire (lower)

// Monitor (Game.Basic.Monitor)
Data.Instance.after.Register(Data.Type.Home, OnHomeChanged);  // register (upper)
Data.Instance.after.Fire(Data.Type.Home, newHomeData);         // fire (lower)
```

**程序集引用规则：**

- .asmdef 的 `references` 必须显式声明所有直接使用的程序集（传递性依赖不自动生效）
- 文件名必须与程序集名称匹配（如 `Utils.asmdef` → `"name": "Utils"`）
- 禁止引用更高层

```json
{ "name": "Logic", "references": ["Net", "Data", "Utils", "Basic", "Framework"] }
```

---

## 三、文件管理规范（File Management）

### 1. 命名约定

| 类型 | 命名模式 | 示例 |
|------|---------|------|
| 核心管理器单例 | 单词命名 | `Data.cs`, `Net.cs`, `UI.cs` |
| 一般管理器 | `{功能}Manager.cs` | `SDKManager.cs`, `ZipManager.cs` |
| 扩展方法 | `{目标类型}Extension.cs` | `ButtonExtension.cs`, `SliderExtension.cs` |
| 工具类 | `{领域}Utils.cs` | `MathUtils.cs`, `PlatformUtils.cs` |
| 处理器 | `{职责}Handler.cs` | `ProtocolHandler.cs` |
| 转换器 | `{源}Converter.cs` | `ProtocolConverter.cs` |
| 数据模型 | 类使用 `{名称}Data` 后缀 | `HomeData`, `MapData`（定义在 `GameData.cs`） |
| UI 界面 | 与 `Config.UI` 键名一致 | `Home.cs`, `Start.cs`, `Story.cs` |
| 平台实现 | `{平台名}Sdk.cs` | `AndroidSdk.cs`, `IOSSdk.cs` |

### 2. 子目录规则

- 同一功能领域的文件达到 **3 个以上**时，建子目录归组
- 子目录命名使用 **PascalCase**
- 禁止嵌套超过 **2 层**（层目录本身算第 1 层）
- 子目录不需要独立 .asmdef（除非有明确的程序集隔离需求）
